// @ts-check
import { defineConfig } from 'astro/config';

// Deployed to GitHub Pages at https://berlingalvan.github.io/portfolio-website
// If you later rename the repo to berlingalvan.github.io, remove `base`
// and change `site` to 'https://berlingalvan.github.io'.
export default defineConfig({
  site: 'https://berlingalvan.github.io',
  base: '/portfolio-website',
});
